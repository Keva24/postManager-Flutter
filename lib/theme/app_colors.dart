import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color scaffold = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color textHint = Color(0xFFAAAAAA);

  // Accent (steel blue)
  static const Color accent = Color(0xFF2C5F8A);
  static const Color accentLight = Color(0xFFD6E4F0);

  // Skeleton shimmer
  static const Color shimmerBase = Color(0xFFE0E0E0);
  static const Color shimmerHigh = Color(0xFFF5F5F5);

  // Danger
  static const Color danger = Color(0xFFB00020);
  static const Color dangerLight = Color(0xFFFDE8EC);

  // Dividers
  static const Color divider = Color(0xFFDDDDDD);
}
